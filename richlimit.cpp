#include <bits/stdc++.h>
#define ord(x) ((int)(x)-48)
#define chr(x) ((char)(x+48))
using namespace std;
typedef long long ll;

// Constants.
const int AlphSz = 2;

// The alphabet.
char alphabet[AlphSz] = {'0', '1'};

// The set of unique nonempty palindromes we have seen up to now.
set<string> upals;

int main()
{
    string x = "0";

    for (i = 0; i <  AlphSz; i++)
    {
        x += alphabet[i];
        Test
    }
    return 0;
}
